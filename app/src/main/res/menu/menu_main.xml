<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <item android:id="@+id/action_settings"
          android:title="@string/action_settings"
          android:orderInCategory="100"
          app:showAsAction="never" />

    <group android:id="@+id/opsOptions">
        <item android:id="@+id/contentResolver"
              android:title="@string/content_resolver"
              android:onClick="chooseOpsOption"/>
        <item android:id="@+id/contentProviderClient"
              android:title="@string/content_provider_client"
              android:onClick="chooseOpsOption" />
    </group>

</menu>
